import Style from "./HomeBanner.module.scss";
const HomeBanner = ({ props }) => {

  return (
    <section className={Style.mdx_banner_sec} >
      Homebanner
    </section>
  );
};

export default HomeBanner;
